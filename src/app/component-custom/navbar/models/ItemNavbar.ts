export class ItemNavbar {
  key: string;
  label: string;
  path: string;

  constructor(key: string, label: string, path: string) {
    this.key = key;
    this.label = label;
    this.path = path;
  }
}
