import { SetDateFormatPipe } from './set-date-format.pipe';

describe('SetDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SetDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
